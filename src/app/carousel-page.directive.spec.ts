import { CarouselPageDirective } from './carousel-page.directive';

describe('CarouselPageDirective', () => {
  it('should create an instance', () => {
    const directive = new CarouselPageDirective();
    expect(directive).toBeTruthy();
  });
});
